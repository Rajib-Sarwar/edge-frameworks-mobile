name: iOS

on:
  push:
    paths:
      - "ios/**"
      - ".github/workflows/ios.yml"
  pull_request:

jobs:
  ios-test:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run Swift tests
        working-directory: ios
        run: swift test

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate iOS example project
        working-directory: ios/Examples/AppleFoundationModelsDemo
        run: xcodegen generate

      - name: Build iOS example
        working-directory: ios/Examples/AppleFoundationModelsDemo
        run: |
          xcodebuild \
            -project AppleFoundationModelsDemo.xcodeproj \
            -scheme AppleFoundationModelsDemo \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build


      - name: Generate Equipory project
        working-directory: ios/Apps/Equipory
        run: xcodegen generate

      - name: Build Equipory
        working-directory: ios/Apps/Equipory
        run: |
          xcodebuild \
            -project Equipory.xcodeproj \
            -scheme Equipory \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build
